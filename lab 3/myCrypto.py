Create a file called myCrypto.py. Implement one of the encryption algorithms using these files. 
Make sure you add comments indicating the type of encryption algorithm used. 
You should have a method called encrypt and a method called decrypt at the minimum in this file.


Import math
Import random

def scrambledecrypt(randomnum)

evenchar = ""
oddchar = ""
charcount = 1

for ch in plainText:  
      if charCount % 2 == 0:  
          evenChars = evenChars + ch  
      else:  
          oddChars = oddChars + ch  
    charCount = charCount + 1  
  cipherText = oddChars + evenChars  

for ch in ciphertext:  
      if charCount % 2 == 0:  
          evenChars = evenChars + ch  
      else:  
          oddChars = oddChars + ch  
    charCount = charCount + 1  
  cipherText = oddChars + evenChars  
  return   
      
     
